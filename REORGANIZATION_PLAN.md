# Reorganization Plan Pointer

Authoritative plan:
- `../REORGANIZATION_PLAN.md`

Current `reorganized/` layout is tooling-first: active code lives in `tools/`, `models/`, `fusion/`, and `feature_representation/`.
`reconciliation/` is reserved for process/audit records (source mapping, move log, duplicate/conflict decisions, phase completion).